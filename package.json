{
  "name": "studyflow-backend",
  "version": "1.0.0",
  "description": "Backend do StudyFlow — proxy seguro para a API Anthropic",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
